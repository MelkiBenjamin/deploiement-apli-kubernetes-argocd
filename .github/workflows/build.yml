name: Build

on:
  push:
    branches:
      - main


jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - run: ls
      
      - run: cp sonar-project.properties  /github/workspace/
      - run: cat  /github/workspace/
      - uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_HOST_URL: https://192.168.0.123:9000
          SONAR_LOGIN: ${{ secrets.SONAR_LOGIN }}
          SONAR.projectKey: pro
        #with:
          # Spécifiez ici les paramètres de l'analyse SonarQube
          #args: >
           # -Dsonar.projectKey=gihub-aks
            #-Dsonar.sources=.
            #-Dsonar.host.url=https://192.168.0.123:9000
            #-Dsonar.login=SONAR_TOKEN: ${{ secrets.SONAR_LOGIN }}
              
      # If you wish to fail your job when the Quality Gate is red, uncomment the
      # following lines. This would typically be used to fail a deployment.
      # - uses: sonarsource/sonarqube-quality-gate-action@master
      #   timeout-minutes: 5
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
